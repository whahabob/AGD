using UnityEngine;
using System.Collections;

public class FloaterScript : MonoBehaviour {

	/*public int maxFloat = 50;
	int currentFloat = 0;
	public float floatSpeed = 0.005f;
	bool reachedMaxFloat = false;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void FixedUpdate () {
		Floater ();
	}

	void Floater() {
		if (!reachedMaxFloat) {
			currentFloat++;
			transform.position = new Vector2 (transform.position.x, transform.position.y + floatSpeed);
		} else if (reachedMaxFloat) {
			currentFloat--;
			transform.position = new Vector2 (transform.position.x, transform.position.y - floatSpeed);
		}

		if (currentFloat >= maxFloat && !reachedMaxFloat) {
			reachedMaxFloat = true;
		} else if (currentFloat <= 0 && reachedMaxFloat) {
			reachedMaxFloat = false;
		}

	}*/
}
